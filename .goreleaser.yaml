# GoReleaser configuration for tinyforge-cli
# Docs: https://goreleaser.com
#
# Asset naming is intentionally STABLE (no version suffix) so the GitHub Action
# auto-submit workflow in student starters can curl
#   https://github.com/tinyforge-cn/tinyforge-cli/releases/latest/download/tinyforge-linux-amd64
# directly. See tinyforge-cli/docs/github-action/workflow-template.yml.

version: 2

project_name: tinyforge

before:
  hooks:
    - go mod tidy

builds:
  - id: tinyforge
    main: ./cmd/tinyforge
    binary: tinyforge
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/tinyforge-cn/cli/internal/version.Version={{ .Version }}
      - -X github.com/tinyforge-cn/cli/internal/version.Commit={{ .ShortCommit }}

# Raw binary uploads (no tar.gz) — keeps `curl ... -o tinyforge && chmod +x`
# usage trivial. Windows variant gets `.exe` appended automatically by GoReleaser.
archives:
  - id: binaries
    formats: [binary]
    name_template: "tinyforge-{{ .Os }}-{{ .Arch }}"

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

snapshot:
  version_template: "{{ incpatch .Version }}-snapshot-{{ .ShortCommit }}"

release:
  draft: false
  prerelease: auto
  name_template: "v{{ .Version }}"
  extra_files:
    - glob: scripts/install.sh
  header: |
    ## tinyforge-cli {{ .Tag }}

    **macOS / Linux:**
    ```bash
    curl -fsSL https://github.com/tinyforge-cn/tinyforge-cli/releases/latest/download/install.sh | sh
    ```

    **Windows (PowerShell):**
    ```powershell
    irm https://github.com/tinyforge-cn/tinyforge-cli/releases/latest/download/tinyforge-windows-amd64.exe -OutFile tinyforge.exe
    ```

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
      - "Merge pull request"
      - "Merge branch"
  groups:
    - title: Features
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: Bug fixes
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: Others
      order: 999
